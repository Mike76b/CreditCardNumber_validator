# CreditCardNumber_validator
`C code.`

(EN)

Implementation of Hans Peter Luhn’s algorithm, used to determine if a credit card number is (syntactically) valid.

(ES)

Implementación del algoritmo de Hans Peter Luhn, usado para determinar si el número de una tarjeta de crédito es válido (sintácticamente).
